import EditMemoryChunkCell from 'src/components/MemoryChunk/EditMemoryChunkCell'

const EditMemoryChunkPage = ({ cuid }) => {
  return <EditMemoryChunkCell cuid={cuid} />
}

export default EditMemoryChunkPage
