{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web"
    ]
  },
  "devDependencies": {
    "@redwoodjs/auth-dbauth-setup": "4.1.1",
    "@redwoodjs/core": "4.1.1"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config",
    "root": true
  },
  "engines": {
    "node": ">=14.19 <=18.x",
    "yarn": ">=1.15"
  },
  "prisma": {
    "seed": "yarn rw exec seed"
  },
  "scripts": {
    "start": "rw dev",
    "dev": "yarn rw prisma db push && yarn rw exec seed && yarn rw dev",
    "testDeploy": "yarn install && yarn rw deploy render web && yarn rw build api"
  },
  "packageManager": "yarn@3.3.1"
}
